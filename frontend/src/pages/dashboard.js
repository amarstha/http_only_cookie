import Layout from '../hocs/Layout';

const Dashboard = () => (
	<Layout
		title='HttpOnly Auth | Dashboard'
		content='Dashboard page for httpOnly Cookies with json web tokens'
	>
		<h1>Dashboard</h1>
	</Layout>
);
export default Dashboard;